# 강의 실시간 채팅 - Streamlit 버전 🐍

Python + Streamlit으로 만든 실시간 채팅 시스템!

## 🎯 특징

- ✅ **완전 Python** - Node.js 불필요
- ✅ **Streamlit Cloud 무료 배포** - 완전 무료!
- ✅ **QR 코드 자동 생성**
- ✅ **실시간 채팅** - 자동 새로고침
- ✅ **강사/학생 분리 인터페이스**
- ✅ **모바일 친화적**

## 🚀 로컬 실행

### 1. 의존성 설치

```bash
cd streamlit-app
pip install -r requirements.txt
```

### 2. 실행

```bash
streamlit run app.py
```

브라우저가 자동으로 열립니다!

## ☁️ Streamlit Cloud 배포 (무료!)

### 1. GitHub 업로드

```bash
cd streamlit-app
git init
git add .
git commit -m "Streamlit 채팅 앱"
git remote add origin https://github.com/사용자명/lecture-chat-streamlit.git
git push -u origin main
```

### 2. Streamlit Cloud 배포

1. **Streamlit Cloud 가입**
   ```
   https://streamlit.io/cloud
   → Sign up (GitHub로)
   ```

2. **New app 생성**
   ```
   "New app" 클릭
   → Repository: lecture-chat-streamlit
   → Branch: main
   → Main file path: app.py
   → Deploy!
   ```

3. **5분 대기**
   ```
   자동 배포 완료!
   ```

4. **URL 확인**
   ```
   https://your-app.streamlit.app
   ```

## 💡 사용 방법

### 강사

1. **세션 시작**
   ```
   앱 실행 → "강사로 시작" 클릭
   → "새 세션 시작" 클릭
   ```

2. **QR 코드 공유**
   ```
   사이드바에 QR 코드 표시됨
   → "QR 코드 저장" 클릭
   → 학생들에게 공유
   ```

3. **채팅 확인 및 답변**
   ```
   실시간으로 학생 질문 확인
   → 답변 입력 → 전송
   ```

### 학생

1. **QR 코드 스캔**
   ```
   스마트폰으로 QR 스캔
   → 브라우저에서 앱 열림
   ```

2. **이름 입력**
   ```
   닉네임 입력 → "채팅 참여"
   ```

3. **질문하기**
   ```
   메시지 입력 → 전송
   ```

## 📊 데이터 저장

현재 버전은 로컬 JSON 파일 사용:
- 간단하고 빠름
- Streamlit Cloud에서도 작동 (세션 중)
- 앱 재시작 시 데이터 유지

**업그레이드 옵션:**
- Firebase Firestore (실시간 동기화)
- SQLite (영구 저장)
- PostgreSQL (대규모)

## 🎨 커스터마이징

### 색상 변경

`.streamlit/config.toml`:
```toml
[theme]
primaryColor = "#667eea"  # 원하는 색상으로
backgroundColor = "#ffffff"
```

### 자동 새로고침 시간

`app.py`:
```python
time.sleep(5)  # 5초 → 원하는 시간으로
```

## 🔄 Node.js 버전과 비교

| 항목 | Node.js + Socket.io | Python + Streamlit |
|------|---------------------|---------------------|
| **언어** | JavaScript | Python ✅ |
| **실시간성** | WebSocket (즉각) | 폴링 (3-5초) |
| **배포** | Railway, Render | Streamlit Cloud ✅ |
| **무료** | 500-750h/월 | 무제한 ✅ |
| **설정** | 복잡 | 매우 쉬움 ✅ |
| **코드** | ~500줄 | ~300줄 ✅ |

## 💰 비용

**Streamlit Cloud:**
- ✅ 완전 무료
- ✅ 무제한 사용
- ✅ GitHub 자동 배포
- ✅ HTTPS 자동

**제한:**
- 1GB RAM (충분함)
- 리소스 공유 (느릴 수 있음)

## 🐛 문제 해결

### "데이터가 사라짐"

**원인:** Streamlit Cloud 재시작

**해결:** Firebase 또는 외부 DB 사용

### "느림"

**원인:** 폴링 방식

**해결:** 
- 자동 새로고침 간격 조정
- Firebase 실시간 DB 사용

### "QR 코드 안 보임"

**원인:** secrets 미설정

**해결:**
```
Streamlit Cloud → App settings → Secrets
→ APP_URL 추가
```

## 🎯 장점

1. ✅ **Python만** - JavaScript 불필요
2. ✅ **완전 무료** - 비용 0원
3. ✅ **쉬운 배포** - 클릭 몇 번
4. ✅ **자동 HTTPS**
5. ✅ **GitHub 통합**

## 📝 다음 단계

### 기능 추가:

- [ ] Firebase 연동 (완벽한 실시간)
- [ ] 채팅 내역 영구 저장
- [ ] 이미지 업로드
- [ ] 투표 기능
- [ ] 통계 대시보드

## 🚀 지금 바로 시작

```bash
cd streamlit-app
pip install -r requirements.txt
streamlit run app.py
```

**5초 만에 시작!** ✨

